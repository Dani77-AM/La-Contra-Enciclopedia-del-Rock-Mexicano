// Guarda el nombre de la página.
let nombrePagina = "Bandas por Década";

// Guarda la década destacada del momento.
let decadaDestacada = "2020s";

// Guarda el número total de décadas mostradas.
let totalDecadas = 7;

// Guarda el número de recomendaciones por década.
let recomendacionesPorDecada = 3;

// Indica si la página tiene enlaces a redes sociales disponibles.
let tieneRedesSociales = true;

// Mostramos los valores en la consola.
console.log(nombrePagina);
console.log(decadaDestacada);
console.log(totalDecadas);
console.log(recomendacionesPorDecada);
console.log(tieneRedesSociales);
